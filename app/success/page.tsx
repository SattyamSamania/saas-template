import { db } from "@/lib/db";
import { formatUsd } from "@/lib/leaderboard";
export const dynamic="force-dynamic";
export default async function Success({searchParams}:{searchParams:Promise<{entry?:string}>}){const {entry}=await searchParams; const e=entry?await db.leaderboardEntry.findUnique({where:{id:entry}}):null; if(!e)return <main><div className="mx-auto max-w-xl py-24 text-center"><h1 className="text-3xl font-black">We couldn't find that entry.</h1><a className="mt-6 inline-block font-bold text-violet-600" href="/">Back to leaderboard</a></div></main>; const all=await db.leaderboardEntry.findMany({where:{status:"PAID",visibility:"VISIBLE"},orderBy:[{amount:"desc"},{paidAt:"asc"}],select:{id:true}}); const rank=e.status==="PAID"?all.findIndex(x=>x.id===e.id)+1:null; return <main className="min-h-screen"><div className="mx-auto max-w-xl py-20"><div className="rounded-[2rem] border border-black/10 bg-white p-8 text-center shadow-xl md:p-12"><div className="text-5xl">👑</div><p className="mt-5 font-bold text-violet-600">PAYMENT CONFIRMED ✓</p><h1 className="mt-2 text-4xl font-black">Welcome to the board.</h1>{e.status!=="PAID"?<p className="mt-6 text-black/55">Your payment is still being confirmed. Refresh this page in a moment.</p>:<><p className="mt-6 text-black/55">You are currently</p><p className="mt-1 text-6xl font-black">#{rank}</p><p className="mt-5 font-bold">{e.walletName} · {formatUsd(e.amount)}</p><p className="mt-2 text-sm text-black/45">Your position can change when someone pays more.</p></>}<div className="mt-8 flex justify-center gap-3"><a href="/" className="rounded-xl bg-black px-5 py-3 font-bold text-white">View leaderboard</a>{e.status==="PAID"&&<a href={`https://twitter.com/intent/tweet?text=${encodeURIComponent(`I just claimed #${rank} on BrandMyWallet 👑`)}&url=${encodeURIComponent(`${process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000"}/?entry=${e.id}`)}`} target="_blank" rel="noopener noreferrer" className="rounded-xl border border-black/10 px-5 py-3 font-bold">Share on X</a>}</div></div></div></main>}
