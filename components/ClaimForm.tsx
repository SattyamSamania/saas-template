"use client";
import { useState } from "react";
export function ClaimForm({ minimum, onClose }: { minimum:number; onClose:()=>void }) {
 const [form,setForm]=useState({name:"",walletName:"",walletAddress:"",websiteUrl:"",twitterUrl:"",imageUrl:"",tagline:""}); const [loading,setLoading]=useState(false); const [error,setError]=useState("");
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError("");try{const r=await fetch('/api/checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw new Error(d.error);window.location.assign(d.checkoutUrl);}catch(err){setError(err instanceof Error?err.message:'Please try again.');setLoading(false)}};
 const field=(key:keyof typeof form,label:string,required=false)=> <label className="block text-sm font-semibold">{label}{required?' *':''}<input required={required} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} maxLength={key==='tagline'?180:key==='walletAddress'?180:80} className="mt-1.5 w-full rounded-xl border border-black/10 bg-white px-3.5 py-3 outline-none focus:border-violet-500" /></label>;
 return <div className="fixed inset-0 z-50 grid place-items-center bg-black/50 p-4" onMouseDown={e=>e.currentTarget===e.target&&onClose()}><form onSubmit={submit} className="max-h-[92vh] w-full max-w-lg overflow-y-auto rounded-3xl bg-[#fafafa] p-6 shadow-2xl md:p-8"><div className="flex items-start justify-between"><div><p className="text-sm font-bold text-violet-600">CLAIM #1</p><h2 className="mt-1 text-2xl font-black">Put your wallet on the board.</h2></div><button type="button" onClick={onClose} className="text-2xl text-black/40">×</button></div><div className="mt-6 space-y-4">{field('name','Your name',true)}{field('walletName','Wallet / Brand name',true)}{field('walletAddress','Wallet address')}{field('websiteUrl','Website URL')}{field('twitterUrl','X / Twitter URL')}{field('imageUrl','Logo URL')}{field('tagline','Short tagline')}</div>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 rounded-2xl bg-[#111] p-4 text-white"><p className="text-xs text-white/50">SERVER-CALCULATED MINIMUM</p><p className="text-2xl font-black">{(minimum/100).toLocaleString('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0})}</p></div><button disabled={loading} className="mt-4 w-full rounded-xl bg-violet-600 py-4 font-bold text-white disabled:opacity-50">{loading?'Creating secure checkout…':`Pay ${(minimum/100).toLocaleString('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0})} & Claim #1`}</button><p className="mt-3 text-center text-xs text-black/40">The final amount is recalculated on the server at checkout.</p></form></div>
}
