import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { entrySchema } from "@/lib/validation";
import { getDodo, getAppUrl } from "@/lib/dodo";
import { calculateMinimumBid } from "@/lib/leaderboard";
const hits=new Map<string,{count:number;reset:number}>();
function limited(ip:string){const now=Date.now(),x=hits.get(ip);if(!x||x.reset<now){hits.set(ip,{count:1,reset:now+60_000});return false}x.count++;return x.count>8}
export async function POST(request:Request){const ip=request.headers.get("x-forwarded-for")?.split(",")[0]?.trim()??"unknown";if(limited(ip))return NextResponse.json({error:"Too many attempts. Please try again shortly."},{status:429});let entryId="";try{const input=entrySchema.parse(await request.json());const entry=await db.$transaction(async tx=>{await tx.checkoutLock.upsert({where:{id:1},create:{id:1},update:{version:{increment:1}}});const top=await tx.leaderboardEntry.findFirst({where:{status:"PAID",visibility:"VISIBLE"},orderBy:[{amount:"desc"},{paidAt:"asc"}],select:{amount:true}});const active=await tx.leaderboardEntry.findFirst({where:{status:"PENDING",expiresAt:{gt:new Date()}},orderBy:{amount:"desc"},select:{amount:true}});const amount=calculateMinimumBid(top?.amount??null,active?.amount??0);return tx.leaderboardEntry.create({data:{name:input.name,walletName:input.walletName,walletAddress:input.walletAddress||null,websiteUrl:input.websiteUrl||null,twitterUrl:input.twitterUrl||null,imageUrl:input.imageUrl||null,tagline:input.tagline||null,amount,currency:"USD",expiresAt:new Date(Date.now()+30*60_000)}})});entryId=entry.id;const productId=process.env.DODO_PRODUCT_ID;if(!productId)throw new Error("DODO_NOT_CONFIGURED");const session=await getDodo().checkoutSessions.create({product_cart:[{product_id:productId,quantity:1,amount:entry.amount}],return_url:`${getAppUrl()}/success?entry=${encodeURIComponent(entry.id)}`,metadata:{entry_id:entry.id,expected_amount:String(entry.amount),currency:"USD"}});await db.leaderboardEntry.update({where:{id:entry.id},data:{dodoCheckoutSessionId:session.session_id}});return NextResponse.json({checkoutUrl:session.checkout_url})}catch(error){if(entryId)await db.leaderboardEntry.updateMany({where:{id:entryId,status:"PENDING"},data:{status:"FAILED"}}).catch(()=>{});console.error("checkout creation failed",error instanceof Error?error.message:"unknown");return NextResponse.json({error:"Something went wrong creating your checkout. Please try again."},{status:400})}}
